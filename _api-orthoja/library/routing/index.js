/**
 * Classes and methods related to routing messages.
 * @module Routing
 */

module.exports = {
    OrthojaRouter: require('./OrthojaRouter').default,
    SetupRoutes: require('./SetupRoutes').default.SetupRoutes
}